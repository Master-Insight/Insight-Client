---
const { cards = [], CardComponent } = Astro.props;
---

<div class="flex flex-wrap gap-8 px-4">
  {
    cards.length > 0 &&
      cards.map((card, index) => <CardComponent key={index} {...card} />)
  }
</div>
